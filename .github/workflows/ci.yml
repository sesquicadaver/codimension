# CI: independent static gates, pytest matrix, wheel install, offscreen GUI smoke
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  # Independent static jobs so one failure does not hide the others (audit P0).
  ruff:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml
      - name: Install lint extras
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -e ".[lint]"
      - name: Ruff check
        run: |
          . .venv/bin/activate
          ruff check codimension cdmplugins

  ruff-format:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml
      - name: Install lint extras
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -e ".[lint]"
      - name: Ruff format check
        run: |
          . .venv/bin/activate
          ruff format --check codimension cdmplugins

  mypy:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml
      - name: Install lint extras
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -e ".[lint]"
      - name: Mypy
        run: |
          . .venv/bin/activate
          mypy $(find codimension cdmplugins -name '*.py' ! -path '*/flowui/everything.py')

  import-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml
      - name: Install and run T072/T085/R103 gates
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -e ".[lint]"
          python scripts/check_package_relative_imports.py
          python scripts/check_core_import_graph.py
          python scripts/check_module_boundaries.py
          python scripts/check_constraints.py

  # R243: Bandit high-severity gate (security extra).
  bandit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml
      - name: Install security extras and run Bandit
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -e ".[security]"
          # High severity only — medium/low tracked in plugin UI / backlog.
          bandit -r codimension -lll -ii -q

  # R243: coverage floor for the installable package (one Python version).
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml
      - name: Pytest with coverage floor
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -e ".[test,tools,security]"
          QT_QPA_PLATFORM=offscreen pytest tests/ -q --tb=line \
            --cov=codimension \
            --cov-report=term \
            --cov-fail-under=28

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml

      - name: Install and pytest
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -e ".[test,tools,security]"
          pip check
          echo "::group::Debugger session e2e (offscreen)"
          QT_QPA_PLATFORM=offscreen pytest tests/debugger/ -m debugger_session -v --tb=short
          echo "::endgroup::"
          pytest tests/ -v --tb=short

  wheel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml

      - name: Build wheel and clean-venv install
        run: |
          python -m venv .venv-build
          . .venv-build/bin/activate
          pip install --upgrade pip build
          python -m build --wheel
          deactivate
          python -m venv .venv-wheel
          . .venv-wheel/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt dist/codimension-*.whl
          pip check
          python -c "import codimension; print(codimension.__file__)"

  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-runtime.txt
            constraints.txt
            pyproject.toml

      - name: Install, import, pip-audit, offscreen GUI smoke
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip
          pip install -c constraints.txt -r requirements.txt
          pip install -c constraints.txt -e .
          python -c "import codimension; import cdmplugins; print('OK')"
          pip install -c constraints.txt pip-audit
          pip-audit -r requirements.txt
          QT_QPA_PLATFORM=offscreen python scripts/offscreen_gui_smoke.py

  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: "3.12"
      - name: Markdown link and README invariants
        run: |
          python scripts/check_docs.py
          python scripts/check_constraints.py

  # Single required status for branch protection (G01).
  ci-gate:
    if: always()
    needs:
      - ruff
      - ruff-format
      - mypy
      - import-gates
      - bandit
      - coverage
      - test
      - wheel
      - smoke
      - docs
    runs-on: ubuntu-latest
    steps:
      - name: Require all CI jobs success
        run: |
          set -euo pipefail
          results=(
            "${{ needs.ruff.result }}"
            "${{ needs.ruff-format.result }}"
            "${{ needs.mypy.result }}"
            "${{ needs.import-gates.result }}"
            "${{ needs.bandit.result }}"
            "${{ needs.coverage.result }}"
            "${{ needs.test.result }}"
            "${{ needs.wheel.result }}"
            "${{ needs.smoke.result }}"
            "${{ needs.docs.result }}"
          )
          for r in "${results[@]}"; do
            echo "need result: $r"
            if [ "$r" != "success" ]; then
              echo "ci-gate failed: required job result=$r"
              exit 1
            fi
          done
          echo "ci-gate OK"
