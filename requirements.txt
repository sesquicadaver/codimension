# Convenience full install for local/CI (runtime + tools + security).
# Canonical runtime deps: pyproject.toml [project.dependencies]
#                          and requirements-runtime.txt (setup.py install_requires)
# Pinned CI snapshot: constraints.txt (regenerate via scripts/generate_constraints.py)
# Groups: pip install -c constraints.txt -e ".[dev]"  (tools, lint, test, security, ssh, mcp)
#
# cdmpyparser/cdmcfparser: C extensions have python_requires='>=3.5,<3.10' on PyPI.
# Excluded — codimension uses built-in pure-Python fallbacks (brief_ast, flow_ast) for 3.10+.
setuptools>=65.0; python_version >= "3.12"
cdmgcplugin>=1.0.4
pylint==2.5.3
cdmsysinfoplugin>=1.0.0
pyflakes>=2.1.1
flake8>=3.7.7
radon>=4.1.0
PyQt5>=5.15.9
python-magic>=0.4.15
qutepart>=3.3.0
yapsy>=1.12.0
jedi>=0.17.2
vulture>=1.6
gprof2dot>=2017.9.19
mistune>=3.2.1
pygments>=2.20.0
qdarkstyle>=2.8.1
packaging>=21.0
paramiko>=3.0
keyring>=23.0
# Optional tool / test / security groups (also in pyproject optional-dependencies)
ruff>=0.1.0
mypy>=1.0.0
pytest>=9.0.3
pytest-cov>=4.0.0
bandit>=1.7.0
pip-audit>=2.0.0

mcp>=2.0,<3
tree-sitter>=0.23,<0.27
tree-sitter-rust>=0.23,<0.25
tree-sitter-cpp>=0.23,<0.24
